import type { NextPage } from "next";
import LoginWallet from "@/components/LoginWallet";

const Home: NextPage = () => {
  return (
    <div>
      <LoginWallet />
    </div>
  );
};

export default Home;
